# Change camera filenames based on the given prefix for Windows written in java (using java 10)

### Development install ( Maven required )

Use ```maven install -f "path/to/pom.xml"``` to install all necessary packages

### Production build ( Maven required )

Use ```mvn package -f "path/to/pom.xml"``` to compile jar

### How it works

   - Choose folder
   - Start listen
